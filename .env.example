# Wiki.js API Configuration
WIKIJS_API_URL=http://localhost:3000/graphql
WIKIJS_API_TOKEN=changeme

# SSL Configuration (optional)
# Set to false to disable SSL verification (for testing only)
WIKIJS_SSL_VERIFY=true

# Path to custom CA certificate bundle (optional)
# WIKIJS_CA_BUNDLE=/path/to/ca-bundle.crt
